# ==============================================================================
# NexTarget Backend - Environment Variables
# ==============================================================================
# Copiez ce fichier en .env et complétez les valeurs

# ==============================================================================
# JWT Configuration (REQUIRED)
# ==============================================================================
# Secret key for signing JWT tokens - MUST be strong in production (min 32 chars)
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=change_me_generate_long_random_min_32_chars

# Token expiration times (optional, defaults provided)
# ACCESS_TOKEN_EXP_MINUTES=60        # Long-lived token for API calls (default: 60 min)
# CALLBACK_TOKEN_EXP_MINUTES=10      # Short-lived token for OAuth redirect (default: 10 min)
# REFRESH_TOKEN_EXP_DAYS=30          # Refresh token lifetime (NT-048, default: 30 days)

# ==============================================================================
# Logging (NT-053)
# ==============================================================================
# Niveau du logger applicatif JSON (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# ==============================================================================
# CORS (NT-065)
# ==============================================================================
# Comma-separated list of allowed browser origins.
# Unset (default): "*" in dev, NO origins outside dev (mobile app unaffected:
# native HTTP calls don't send an Origin header).
# Only set this if a web client exists.
# CORS_ALLOW_ORIGINS=https://app.example.com,https://admin.example.com

# ==============================================================================
# Read-only Administration (NT-049, OPTIONAL)
# ==============================================================================
# Both values are required to enable the HTML page GET /app/admin/users. Never put the plain
# password in this file or in an environment variable. Generate the salted
# verifier locally as documented in docs/guides/admin-read-only.md.
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD_HASH=scrypt$16384$8$1$<salt>$<derived-key>

# ==============================================================================
# Google OAuth Configuration (REQUIRED for Google login)
# ==============================================================================
# Get credentials from: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID (Type: Web application)
# 2. Add authorized redirect URI
# 3. Copy Client ID and Client Secret

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-client-secret

# Redirect URI (must match Google Console configuration)
# Development: http://localhost:8000/auth/google/callback
# Production: https://api.nextarget.app/auth/google/callback
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

# ==============================================================================
# Facebook OAuth Configuration (Optional)
# ==============================================================================
# Get credentials from: https://developers.facebook.com/apps

FACEBOOK_CLIENT_ID=your-facebook-app-id
FACEBOOK_CLIENT_SECRET=your-facebook-app-secret
FACEBOOK_REDIRECT_URI=http://localhost:8000/auth/facebook/callback

# ==============================================================================
# Database Configuration (NT-071)
# ==============================================================================
# Development / unit tests (SQLite - default, no extra setup needed)
# DATABASE_URL=sqlite:///./data.db

# Production (PostgreSQL on Neon — https://neon.tech):
# - DATABASE_URL: pooled connection string of the least-privilege runtime role
#   (e.g. nextarget_app). Used by the app at request time.
# - DATABASE_MIGRATION_URL: direct (unpooled) connection string of the owner
#   role (neondb_owner). Used only by Alembic migrations and administrative
#   operations (pg_dump backup/restore) — never by the running app.
# Both values are Neon-provided connection strings (postgresql://user:pass@host/db
# with sslmode=require) and MUST be set as real secrets in Render/your shell —
# never committed here, never logged.
# DATABASE_URL=postgresql://nextarget_app:***@***-pooler.***.neon.tech/neondb?sslmode=require
# DATABASE_MIGRATION_URL=postgresql://neondb_owner:***@***.neon.tech/neondb?sslmode=require
